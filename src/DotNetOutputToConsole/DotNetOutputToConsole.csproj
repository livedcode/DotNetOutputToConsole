<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props" Condition="Exists('$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props')" />
  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>
    <ProjectGuid>{5FE20856-E888-465D-AD6A-F939E7F37774}</ProjectGuid>
    <OutputType>Library</OutputType>
    <AppDesignerFolder>Properties</AppDesignerFolder>
    <RootNamespace>DotNetOutputToConsole</RootNamespace>
    <AssemblyName>DotNetOutputToConsole</AssemblyName>
    <TargetFrameworkVersion>v4.8.1</TargetFrameworkVersion>
    <FileAlignment>512</FileAlignment>
    <Deterministic>true</Deterministic>
    <!-- NuGet Package Version -->
    <Version>1.1.0</Version>
    <!-- DLL Assembly Versions -->
    <AssemblyVersion>1.1.0.0</AssemblyVersion>
    <FileVersion>1.1.0.0</FileVersion>
    <InformationalVersion>1.1.0</InformationalVersion>
    <!-- NuGet Metadata -->
    <GeneratePackageOnBuild>false</GeneratePackageOnBuild>
    <PackageId>DotNetOutputToConsole</PackageId>
    <Authors>lived</Authors>
    <Company>
    </Company>
    <Description>ASP.NET 4.8+ helper library that outputs variables and errors directly to the browser console for debugging on UAT/DEV servers.</Description>
    <PackageTags>aspnet net48 console logger debug error output</PackageTags>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <RepositoryUrl>https://github.com/livedcode/DotNetOutputToConsole</RepositoryUrl>
    <PackageProjectUrl>https://github.com/livedcode/DotNetOutputToConsole</PackageProjectUrl>
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
    <EmbedUntrackedSources>true</EmbedUntrackedSources>
    <PackageOutputPath>bin\Release</PackageOutputPath>
    <!-- Recommended for NuGet publishing -->
    <RepositoryType>git</RepositoryType>
    <PackageRequireLicenseAcceptance>false</PackageRequireLicenseAcceptance>
    <PackageReleaseNotes>Version 1.1.0 - Improved no-dependency version using HttpUtility.JavaScriptStringEncode.</PackageReleaseNotes>
  </PropertyGroup>
  <!-- Debug -->
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <OutputPath>bin\Debug\</OutputPath>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>
  <!-- Release -->
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
    <DebugType>pdbonly</DebugType>
    <Optimize>true</Optimize>
    <OutputPath>bin\Release\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
    <!-- enable pack on Release -->
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
  </PropertyGroup>
  <!-- References -->
  <ItemGroup>
    <Reference Include="System" />
    <Reference Include="System.Configuration" />
    <Reference Include="System.Web" />
  </ItemGroup>
  <!-- Code files -->
  <ItemGroup>
    <Compile Include="DotNetOutputToConsoleHttpModule.cs" />
    <Compile Include="DotNetOutputToConsoleLogger.cs" />
    <Compile Include="Properties\AssemblyInfo.cs" />
  </ItemGroup>
  <!-- Non-code files -->
  <ItemGroup>
    <None Include="packages.config" />
    <None Include="LICENSE" Pack="true" PackagePath="" />
    <None Include="README.md" />
  </ItemGroup>
  <Import Project="$(MSBuildToolsPath)\Microsoft.CSharp.targets" />
  <!-- Import NuGet Build Tasks Pack 6.14.0 (optional if present on build machine) -->
  <Import Project="$(NuGetPackageRoot)\nuget.build.tasks.pack\6.14.0\build\NuGet.Build.Tasks.Pack.targets" Condition="Exists('$(NuGetPackageRoot)\nuget.build.tasks.pack\6.14.0\build\NuGet.Build.Tasks.Pack.targets')" />
</Project>